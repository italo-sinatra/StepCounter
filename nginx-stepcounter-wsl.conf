# Configuração Nginx para StepCounter no WSL (Windows)
# Permite acesso de outras máquinas na rede local

server {
    # Escuta na porta 80 (HTTP)
    listen 80;
    # Permite acesso de qualquer IP (máquinas na rede)
    listen [::]:80;
    
    # Nome do servidor (pode usar IP ou hostname)
    server_name _;
    
    # IMPORTANTE: Ajuste este caminho para o caminho do seu projeto no WSL
    # Exemplo: /mnt/c/Users/SeuUsuario/Downloads/StepCounter-main/dist
    # Substitua "SeuUsuario" pelo seu nome de usuário do Windows
    root /mnt/c/Users/SeuUsuario/Downloads/StepCounter-main/dist;
    index index.html;
    
    # Logs (caminhos padrão do Nginx no Ubuntu/WSL)
    access_log /var/log/nginx/stepcounter-access.log;
    error_log /var/log/nginx/stepcounter-error.log;
    
    # Configuração para SPA (Single Page Application)
    # Todas as rotas devem servir index.html
    location / {
        try_files $uri $uri/ /index.html;
        
        # Headers de segurança
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
    }
    
    # Cache para arquivos estáticos (assets)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    # Configuração para arquivos de assets do Vite
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    # Desabilitar acesso a arquivos ocultos
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # Configuração de compressão
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;
    
    # Tamanho máximo de upload
    client_max_body_size 10M;
}

